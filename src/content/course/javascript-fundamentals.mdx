---
title: "JavaScript Fundamentals"
description: "Master the core concepts of JavaScript programming"
pubDate: 2024-01-01
category: "frontend"
author: "Aryan Singh"
tags: ["javascript", "frontend", "fundamentals"]
priority: 2  # Second in Module 2
moduleNumber: 2  # HTML/CSS Module
---

### JavaScript Fundamentals: A Comprehensive Guide to Variables, Functions, DOM Manipulation, Async/Await, APIs, and More  

JavaScript is the backbone of modern web development, enabling dynamic and interactive web experiences. Whether you're a beginner or looking to solidify your understanding, mastering JavaScript fundamentals is essential. In this end-to-end guide, we'll cover everything from **variables** and **functions** to **DOM manipulation**, **async/await**, and **APIs**, complete with detailed explanations and code samples.  

---

### 1. Variables and Data Types  

Variables are used to store data in JavaScript. They can hold different types of data, such as numbers, strings, booleans, objects, and more.  

#### Declaring Variables  
JavaScript provides three ways to declare variables:  
- **`var`**: Function-scoped (avoid using in modern code).  
- **`let`**: Block-scoped, allows reassignment.  
- **`const`**: Block-scoped, cannot be reassigned.  

#### Example: Variables and Data Types  
```javascript
// Numbers
let age = 25;
const PI = 3.14;

// Strings
let name = "John Doe";
const greeting = `Hello, ${name}!`; // Template literals

// Booleans
let isStudent = true;

// Arrays
let colors = ["red", "green", "blue"];

// Objects
let person = {
  name: "Alice",
  age: 30,
  isStudent: false,
};

console.log(greeting); // Output: Hello, John Doe!
```

---

### 2. Functions  

Functions are reusable blocks of code that perform a specific task. They can take inputs (parameters) and return outputs.  

#### Function Declaration  
```javascript
function add(a, b) {
  return a + b;
}

console.log(add(2, 3)); // Output: 5
```

#### Function Expression  
```javascript
const multiply = function (a, b) {
  return a * b;
};

console.log(multiply(2, 3)); // Output: 6
```

#### Arrow Functions (ES6)  
Arrow functions provide a concise syntax for writing functions.  
```javascript
const divide = (a, b) => a / b;
console.log(divide(10, 2)); // Output: 5
```

---

### 3. DOM Manipulation  

The Document Object Model (DOM) is a programming interface for HTML and XML documents. JavaScript can manipulate the DOM to dynamically update content, styles, and structure.  

#### Selecting Elements  
```javascript
// Select by ID
const header = document.getElementById("header");

// Select by class
const items = document.getElementsByClassName("item");

// Select by tag
const paragraphs = document.getElementsByTagName("p");

// Query Selector (CSS-style selectors)
const button = document.querySelector("#submit-button");
const allButtons = document.querySelectorAll(".btn");
```

#### Updating Content  
```javascript
header.textContent = "Welcome to JavaScript!";
header.innerHTML = "<h1>Welcome to <strong>JavaScript</strong>!</h1>";
```

#### Adding and Removing Elements  
```javascript
// Create a new element
const newParagraph = document.createElement("p");
newParagraph.textContent = "This is a new paragraph.";

// Append to the body
document.body.appendChild(newParagraph);

// Remove an element
const oldParagraph = document.querySelector("p");
oldParagraph.remove();
```

#### Event Listeners  
```javascript
button.addEventListener("click", () => {
  alert("Button clicked!");
});
```

---

### 4. Asynchronous JavaScript  

JavaScript is single-threaded, but it can handle asynchronous operations using **callbacks**, **promises**, and **async/await**.  

#### Callbacks  
A callback is a function passed as an argument to another function and executed after some operation.  
```javascript
function fetchData(callback) {
  setTimeout(() => {
    callback("Data fetched!");
  }, 2000);
}

fetchData((data) => {
  console.log(data); // Output after 2 seconds: Data fetched!
});
```

#### Promises  
Promises represent the eventual completion (or failure) of an asynchronous operation.  
```javascript
const fetchData = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve("Data fetched!");
  }, 2000);
});

fetchData
  .then((data) => {
    console.log(data); // Output after 2 seconds: Data fetched!
  })
  .catch((error) => {
    console.error(error);
  });
```

#### Async/Await  
`async/await` provides a cleaner way to work with promises.  
```javascript
async function fetchData() {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("Data fetched!");
    }, 2000);
  });
}

async function main() {
  try {
    const data = await fetchData();
    console.log(data); // Output after 2 seconds: Data fetched!
  } catch (error) {
    console.error(error);
  }
}

main();
```

---

### 5. Working with APIs  

APIs (Application Programming Interfaces) allow you to interact with external services and fetch data.  

#### Fetch API  
The Fetch API is a modern way to make HTTP requests.  
```javascript
async function fetchUserData() {
  try {
    const response = await fetch("https://jsonplaceholder.typicode.com/users/1");
    if (!response.ok) {
      throw new Error("Network response was not ok");
    }
    const data = await response.json();
    console.log(data); // Output: User data object
  } catch (error) {
    console.error("Error fetching data:", error);
  }
}

fetchUserData();
```

#### Example: Displaying API Data in the DOM  
```javascript
async function displayUserData() {
  const response = await fetch("https://jsonplaceholder.typicode.com/users/1");
  const user = await response.json();

  const userInfo = document.createElement("div");
  userInfo.innerHTML = `
    <h2>${user.name}</h2>
    <p>Email: ${user.email}</p>
    <p>Phone: ${user.phone}</p>
  `;

  document.body.appendChild(userInfo);
}

displayUserData();
```

---

### 6. Error Handling  

Proper error handling ensures your application can gracefully handle unexpected issues.  

#### Try/Catch  
```javascript
try {
  // Code that may throw an error
  const result = 10 / 0;
  if (!isFinite(result)) {
    throw new Error("Division by zero!");
  }
} catch (error) {
  console.error("An error occurred:", error.message);
}
```

#### Error Handling with Promises  
```javascript
fetch("https://jsonplaceholder.typicode.com/invalid-url")
  .then((response) => {
    if (!response.ok) {
      throw new Error("Network response was not ok");
    }
    return response.json();
  })
  .catch((error) => {
    console.error("Fetch error:", error);
  });
```

---

### 7. Modules (ES6)  

JavaScript modules allow you to split your code into reusable, organized files.  

#### Exporting Modules  
```javascript
// math.js
export function add(a, b) {
  return a + b;
}

export const PI = 3.14;
```

#### Importing Modules  
```javascript
// main.js
import { add, PI } from "./math.js";

console.log(add(2, 3)); // Output: 5
console.log(PI); // Output: 3.14
```

---

### Conclusion  

JavaScript is a versatile and powerful language that forms the foundation of modern web development. By mastering the fundamentals—**variables**, **functions**, **DOM manipulation**, **async/await**, **APIs**, and more—you'll be well-equipped to build dynamic and interactive web applications.  

Here's a quick recap of what we covered:  
1. **Variables and Data Types**: Store and manipulate data.  
2. **Functions**: Create reusable blocks of code.  
3. **DOM Manipulation**: Dynamically update web pages.  
4. **Asynchronous JavaScript**: Handle async operations with callbacks, promises, and async/await.  
5. **APIs**: Fetch and display data from external services.  
6. **Error Handling**: Gracefully handle unexpected issues.  
7. **Modules**: Organize your code into reusable files.  

With these skills, you're ready to take your JavaScript knowledge to the next level. Happy coding!  

---

### Final Code Example: Combining Everything  

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>JavaScript Fundamentals</title>
</head>
<body>
  <h1 id="header">JavaScript Fundamentals</h1>
  <button id="fetch-button">Fetch User Data</button>
  <div id="user-info"></div>

  <script>
    // Variables and Data Types
    const user = {
      name: "John Doe",
      age: 30,
    };

    // DOM Manipulation
    const header = document.getElementById("header");
    header.textContent = `Welcome, ${user.name}!`;

    // Event Listeners
    const fetchButton = document.getElementById("fetch-button");
    const userInfo = document.getElementById("user-info");

    fetchButton.addEventListener("click", async () => {
      try {
        const response = await fetch("https://jsonplaceholder.typicode.com/users/1");
        if (!response.ok) {
          throw new Error("Network response was not ok");
        }
        const data = await response.json();
        userInfo.innerHTML = `
          <h2>${data.name}</h2>
          <p>Email: ${data.email}</p>
          <p>Phone: ${data.phone}</p>
        `;
      } catch (error) {
        console.error("Error fetching data:", error);
      }
    });
  </script>
</body>
</html>
```

This example combines variables, DOM manipulation, event listeners, and API fetching into a single, interactive webpage. Use it as a starting point for your own projects!