---
title: "React Mastery"
description: "Master React.js fundamentals and advanced concepts"
pubDate: 2024-01-01
category: "frontend"
author: "Aryan Singh"
tags: ["react", "frontend", "javascript"]
priority: 3  # Third in Module 2
moduleNumber: 2  # HTML/CSS Module
---

### React Crash Course: Components, Hooks (useState, useEffect), and React Router  

React is a powerful JavaScript library for building user interfaces. It allows you to create reusable components, manage state efficiently, and build single-page applications (SPAs) with ease. In this crash course, we'll cover the fundamentals of React, including **components**, **hooks (useState, useEffect)**, and **React Router**, complete with detailed explanations and code samples.  

---

### 1. Setting Up a React Project  

Before diving into React, you'll need to set up a project. The easiest way to do this is by using **Create React App**.  

#### Install Create React App  
```bash
npx create-react-app my-react-app
cd my-react-app
npm start
```

This will create a new React project and start a development server at `http://localhost:3000`.  

---

### 2. Components  

Components are the building blocks of a React application. They are reusable, self-contained pieces of code that return JSX (JavaScript XML) to describe what should appear on the screen.  

#### Functional Components  
Functional components are JavaScript functions that return JSX.  

```javascript
// src/components/Header.js
import React from "react";

function Header() {
  return (
    <header>
      <h1>Welcome to My React App</h1>
    </header>
  );
}

export default Header;
```

#### Class Components  
Class components are ES6 classes that extend `React.Component`. While functional components are preferred in modern React, class components are still worth knowing.  

```javascript
// src/components/Footer.js
import React, { Component } from "react";

class Footer extends Component {
  render() {
    return (
      <footer>
        <p>&copy; 2023 My React App. All rights reserved.</p>
      </footer>
    );
  }
}

export default Footer;
```

#### Using Components  
You can use components by importing and including them in other components.  

```javascript
// src/App.js
import React from "react";
import Header from "./components/Header";
import Footer from "./components/Footer";

function App() {
  return (
    <div>
      <Header />
      <main>
        <p>This is the main content of my React app.</p>
      </main>
      <Footer />
    </div>
  );
}

export default App;
```

---

### 3. Hooks  

Hooks are functions that allow you to "hook into" React state and lifecycle features from functional components. The two most commonly used hooks are `useState` and `useEffect`.  

#### useState  
`useState` allows you to add state to functional components.  

```javascript
// src/components/Counter.js
import React, { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <p>You clicked {count} times</p>
      <button onClick={() => setCount(count + 1)}>Click me</button>
    </div>
  );
}

export default Counter;
```

#### useEffect  
`useEffect` allows you to perform side effects (e.g., fetching data, updating the DOM) in functional components.  

```javascript
// src/components/UserData.js
import React, { useState, useEffect } from "react";

function UserData() {
  const [user, setUser] = useState(null);

  useEffect(() => {
    fetch("https://jsonplaceholder.typicode.com/users/1")
      .then((response) => response.json())
      .then((data) => setUser(data));
  }, []); // Empty dependency array means this runs once on mount

  if (!user) {
    return <p>Loading...</p>;
  }

  return (
    <div>
      <h2>{user.name}</h2>
      <p>Email: {user.email}</p>
      <p>Phone: {user.phone}</p>
    </div>
  );
}

export default UserData;
```

---

### 4. React Router  

React Router is a library for handling navigation in React applications. It allows you to create single-page applications with multiple views.  

#### Install React Router  
```bash
npm install react-router-dom
```

#### Setting Up Routes  
```javascript
// src/App.js
import React from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";
import Home from "./components/Home";
import About from "./components/About";
import Contact from "./components/Contact";

function App() {
  return (
    <Router>
      <nav>
        <ul>
          <li>
            <Link to="/">Home</Link>
          </li>
          <li>
            <Link to="/about">About</Link>
          </li>
          <li>
            <Link to="/contact">Contact</Link>
          </li>
        </ul>
      </nav>

      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>
    </Router>
  );
}

export default App;
```

#### Creating Route Components  
```javascript
// src/components/Home.js
import React from "react";

function Home() {
  return (
    <div>
      <h2>Home</h2>
      <p>Welcome to the home page!</p>
    </div>
  );
}

export default Home;
```

```javascript
// src/components/About.js
import React from "react";

function About() {
  return (
    <div>
      <h2>About</h2>
      <p>Learn more about us.</p>
    </div>
  );
}

export default About;
```

```javascript
// src/components/Contact.js
import React from "react";

function Contact() {
  return (
    <div>
      <h2>Contact</h2>
      <p>Get in touch with us.</p>
    </div>
  );
}

export default Contact;
```

---

### 5. Putting It All Together  

Here's a complete example that combines components, hooks, and React Router:  

```javascript
// src/App.js
import React, { useState, useEffect } from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";
import Home from "./components/Home";
import About from "./components/About";
import Contact from "./components/Contact";
import Counter from "./components/Counter";
import UserData from "./components/UserData";

function App() {
  return (
    <Router>
      <nav>
        <ul>
          <li>
            <Link to="/">Home</Link>
          </li>
          <li>
            <Link to="/about">About</Link>
          </li>
          <li>
            <Link to="/contact">Contact</Link>
          </li>
        </ul>
      </nav>

      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>

      <Counter />
      <UserData />
    </Router>
  );
}

export default App;
```

---

### Conclusion  

In this crash course, we covered the fundamentals of React, including:  
1. **Components**: Building reusable UI elements.  
2. **Hooks**: Managing state and side effects with `useState` and `useEffect`.  
3. **React Router**: Handling navigation in single-page applications.  

With these concepts, you're well on your way to building dynamic and interactive web applications using React. Keep practicing, and soon you'll be creating complex applications with ease!  

---

### Final Code Example  

Here's the complete code for a simple React app with components, hooks, and routing:  

```javascript
// src/App.js
import React, { useState, useEffect } from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";
import Home from "./components/Home";
import About from "./components/About";
import Contact from "./components/Contact";
import Counter from "./components/Counter";
import UserData from "./components/UserData";

function App() {
  return (
    <Router>
      <nav>
        <ul>
          <li>
            <Link to="/">Home</Link>
          </li>
          <li>
            <Link to="/about">About</Link>
          </li>
          <li>
            <Link to="/contact">Contact</Link>
          </li>
        </ul>
      </nav>

      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/about" element={<About />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>

      <Counter />
      <UserData />
    </Router>
  );
}

export default App;
```

This example combines everything we've learned into a single, cohesive application. Use it as a starting point for your own React projects!